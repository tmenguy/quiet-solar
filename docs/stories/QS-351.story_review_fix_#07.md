# QS-351 — Review fix plan #07

## Summary
- Source PR: #354 (head `601e3db3`, after review-fix #06)
- Source story: docs/stories/QS-351.story.md
- Findings to fix: 1 (must-fix); 8 nice-to-have skipped by user decision (recorded as a PR comment)
- Next implement phase: `implement-task`

Verify-task round 7 roster (opus): edge-case-hunter + regression-proof + coderabbit.
Production code certified: AST-identical to `9735f46d` (comments only); round-6 SF-1
CLOSED; multiplier pin discriminates; 6000-matrix oracle sweep incl. m<n and
unauthorised cells: 0 discrepancies; 229 passed; all 8 CodeRabbit threads resolved.
No should-fix.

## Findings to fix

### [must-fix] MF-1 — `ruff format --check` fails on `home.py`
- File: `custom_components/quiet_solar/ha_model/home.py:2712-2714`
- Severity: must-fix (CI quality gate `check_ruff_format` is red on head; `main` is clean; PR body ticks "Ruff passes")
- Source: qs-review-regression-proof
- Description: The wrapped block introduced by review-fix #05
  ```python
  penalty = (
      len(p_s) * (E_max + 1.0 + PLUGGED_COVERED_CAR_PENALTY_WH) + PASS2_PREFERRED_CAR_OFFSET_EPS_WH
  )
  ```
  is collapsed by ruff to a single 119-char line (line-length 120, `pyproject.toml`).
  `ruff format --check custom_components/quiet_solar` → "Would reformat: …/home.py" (exit 1).
- Proposed fix: `ruff format custom_components/quiet_solar/ha_model/home.py`
  (AST-identical, zero behaviour change). For house-style consistency also
  `ruff format tests/test_person_car_allocation.py` (`:992`, `:1037` non-conforming;
  outside the gate's SRC_DIR so not blocking). Re-run
  `python scripts/qs/quality_gate.py --impacted` before commit. No test change,
  no red-test protocol (formatting only).

## Skipped (user decision, round 7 — recorded on PR #354)

Eight nice-to-have prose/docstring/transcript polish items were skipped to end the
review loop (each round's "fix all" regenerated nits in the previous round's new
text). They are listed verbatim in the round-7 verify comment on PR #354.

## How to apply

Run `implement-task`: format the file, run the impacted gate, push. Then re-run
`/verify-task` — expected to fast-path to `finish-task`.
