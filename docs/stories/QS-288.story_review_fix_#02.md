# QS-288 — Review fix plan #02

## Summary
- Source PR: #297
- Source story: docs/stories/QS-288.story.md
- Findings to fix: 13
- Next implement phase: `/implement-setup-task`

Review context (round 2, at PR head `b8ce78be`): all 16 round-1
findings verified applied (blind-hunter: F1–F14 in diff;
acceptance-auditor: F15/F16 live on GitHub) — zero recurrences. All
four reviewers reported this round, including CodeRabbit (full review,
no Critical). Orchestrator doc-maintenance audit clean
(`check_doc_drift.py` exit 0). Orchestrator fact-check for G3: batch
commits `8b85e385`…`203d7bf1` each carried the previous batch's §5 row
(+1 line) and `4f4bddf0` finalized — the report's cadence note is
correct; round-1 F14's wording was the imprecise side.

Schema policy adopted for G4/G5 (keep the report self-consistent):
entries with a real disposition (fixed-inline / follow-up / wontfix /
REJECTED-for-S) keep the full schema, with `—` placeholders only for
genuinely inapplicable scoring fields; clean results are NOT findings
and become explicit no-findings notes (AC4 style).

## Findings to fix

### [must-fix] G1 — CLAUDE.md paragraph breaks: line-leading `+ ` renders as a bullet
- File: `CLAUDE.md:48-49` ("Quality gate" section)
- Severity: must-fix
- Source: qs-review-blind-hunter
- Description: the round-1 F11 edit wraps so a line begins
  `+ changed-line 100% coverage, self-healing); …`. In CommonMark/GFM
  a line-leading `+ ` is a bullet-list marker and bullet lists may
  interrupt paragraphs, so the durable top-level doc renders as a
  paragraph ending at "(testmon-selected tests" followed by a stray
  bullet.
- Proposed fix: rewrap the sentence so the `+` never starts a line
  (e.g. end the previous line with `tests +`) or write "plus".
  Coordinate with G2 — same sentence is being reworded anyway; do both
  in one edit.

### [should-fix] G2 — Translations-in-CI overclaim in three files
- File: `.claude/commands/implement-task.md:22-24`, `CLAUDE.md:47-50`,
  `AGENTS.md:32`
- Severity: should-fix
- Source: qs-review-edge-case-hunter
- Description: the new CI-parity wording asserts the whole-suite gate —
  "pytest 100% cov + ruff + mypy + translations" — "runs
  authoritatively in CI on every PR", but
  `.github/workflows/pr-quality.yml` has no translations value-check
  (Ruff / MyPy / pytest-cov / HACS only). This PR's own audit report
  records exactly that gap as D-14, filed as follow-up #292 and not
  yet landed. Failure mode: a stale `en.json` value passes `--impacted`
  and passes CI while the docs claim CI provided the net.
- Proposed fix: in all three files, drop "translations" from the
  CI clause or qualify it, e.g. "…runs authoritatively in CI on every
  PR (translations value-check is local-full-gate only until #292
  lands)". Keep the three files' wording mutually consistent.

### [should-fix] G3 — §5 cadence note contradicts fix plan #01/F14; report is the correct side
- File: `docs/stories/QS-288.audit-report.md:588-592` (§5 "Ledger cadence" note)
- Severity: should-fix
- Source: qs-review-blind-hunter (fact-checked by orchestrator)
- Description: the §5 note ("each batch's row … rode into the *next*
  commit; final row + §6 landed in `4f4bddf0`") contradicts the
  committed fix plan #01's F14 description ("one consolidated ledger
  commit (4f4bddf0) after all six batches"). Git history confirms the
  report is right and F14 was imprecise.
- Proposed fix: append a half-line to the §5 cadence note:
  "(supersedes the imprecise 'single consolidated commit'
  characterization in review fix plan #01/F14)". Do this in the same
  edit as G7's pointer line.

### [should-fix] G4 — S-10 missing the S-finding schema fields
- File: `docs/stories/QS-288.audit-report.md:556-564` (S-10)
- Severity: should-fix
- Source: qs-review-acceptance-auditor
- Description: AC5 requires every S-finding to carry ID, cost,
  frequency assumption, estimated saving, effort, score, disposition.
  S-10 — the AC6-mandated proposed-or-rejected profiling entry —
  carries only ID + REJECTED + rationale. Rejection is a real
  disposition, so per the schema policy above it stays an S-entry with
  explicit placeholders.
- Proposed fix: add one line to S-10, e.g.
  `Cost/frequency/saving/score: — (rejected; informational, not a
  seconds-saving change). Effort —.`

### [should-fix] G5 — D-22 is a clean result formatted as a D-finding (CodeRabbit Major)
- File: `docs/stories/QS-288.audit-report.md:416-425` (D-22), plus
  ripple sites (see below)
- Severity: should-fix
- Source: qs-review-coderabbit (🟠 Major, Maintainability)
- Description: D-22 uses `Effort —` and "no action (clean result)",
  which is not a disposition in the report's declared triad
  (fixed-inline / follow-up / wontfix). Per the schema policy above, a
  clean result is not a finding.
- Proposed fix: restructure D-22 as an explicit no-findings note —
  heading like "**No findings — Legacy quarantine: CLEAN.**" (keep the
  evidence text; drop the D-ID and schema fields). Then sweep the
  ripples, keeping every count/reference consistent:
  1. `grep -n "D-22" docs/stories/QS-288.audit-report.md` — update all
     cross-references (e.g. §1 exec summary, B4 section intro).
  2. Count claims: report §1 and the **PR #297 body** say
     "22 discrepancy findings" — change to "21 discrepancy findings
     (+1 explicit-clean legacy result)" or equivalent. Update the PR
     body via `gh pr edit 297 --body …` (edit only that phrase).
  3. Do NOT renumber other findings; D-21 keeps its full schema (it
     has a real wontfix disposition).

### [nice-to-have] G6 — D-10 disposition outside the triad vocabulary
- File: `docs/stories/QS-288.audit-report.md:290` (D-10)
- Severity: nice-to-have
- Source: qs-review-acceptance-auditor
- Description: D-10's disposition reads "relabel now + follow-up" —
  both halves are recorded and verified executed, but the phrasing is
  outside the declared fixed-inline/follow-up/wontfix triad, hurting
  machine-checkability.
- Proposed fix: normalize to the triad, e.g.
  `**Proposed: follow-up** (#293); relabel executed immediately (see
  §6 relabel note).`

### [nice-to-have] G7 — §5 pointer to the review-round commits
- File: `docs/stories/QS-288.audit-report.md` (§5)
- Severity: nice-to-have
- Source: qs-review-acceptance-auditor
- Description: the two post-ledger review-round commits (`8aae289f`
  plan, `b8ce78be` apply) are traceable only via the fix-plan file,
  not §5 — the report isn't fully self-contained as the audit's single
  ledger.
- Proposed fix: one line in §5, e.g. "Review-round fixes: see
  `docs/stories/QS-288.story_review_fix_#01.md` → `b8ce78be` (and
  `…_#02.md` → its apply commit, added when applied)." Same edit as
  G3.

### [nice-to-have] G8 — Retired legacy script needs an active guard
- File: `legacy/scripts/worktree-cleanup.sh:2-4`
- Severity: nice-to-have
- Source: qs-review-edge-case-hunter
- Description: the retirement header is advisory-only; the body still
  performs `git worktree remove --force` plus local-branch deletion,
  and `bash legacy/scripts/worktree-cleanup.sh <n>` runs it regardless
  of the exec bit. Frozen story docs (QS-173, QS-legacy-*) still name
  the script, so a human can find and run it against a worktree with
  uncommitted work behind a single "press Enter" prompt.
- Proposed fix: add an active guard directly under the retirement
  header, keeping the historical body intact below it:
  `echo "RETIRED — use: python scripts/qs/cleanup_worktree.py" >&2; exit 1`
  Verify the legacy-quarantine test still passes (it scans only
  opencode agent bodies).

### [nice-to-have] G9 — Retired script's usage comment cites the pre-move path
- File: `legacy/scripts/worktree-cleanup.sh:9`
- Severity: nice-to-have
- Source: qs-review-edge-case-hunter
- Description: `# Usage: bash scripts/worktree-cleanup.sh
  <issue_number>` still cites the pre-move location; copy-pasting from
  repo root yields "No such file or directory". (The runtime
  `$0`-based usage message is fine.)
- Proposed fix: update the comment to the `legacy/scripts/…` path or
  delete it in favor of the RETIRED header — fold into the same edit
  as G8.

### [nice-to-have] G10 — D-11 evidence still says "bool-returning"
- File: `docs/stories/QS-288.audit-report.md:285` (D-11)
- Severity: nice-to-have
- Source: qs-review-blind-hunter
- Description: D-11's evidence says "a bool-returning
  `probe_if_command_set`", but round-1 F3 established the source truth
  is `bool | None` (None = inconclusive) and ha-battery.md now
  documents the tri-state — one-word residual inaccuracy.
- Proposed fix: change to "a `bool | None`-returning
  `probe_if_command_set`".

### [nice-to-have] G11 — Fix plan #01's F10 description has broken backtick nesting
- File: `docs/stories/QS-288.story_review_fix_#01.md:141` (F10 description)
- Severity: nice-to-have
- Source: qs-review-blind-hunter
- Description: the inline code span `` `( `--impacted`` `` has broken
  backtick nesting and renders garbled — in a finding that was itself
  about garbled formatting. User opted to fix despite the
  frozen-artifact caveat.
- Proposed fix: rewrite the snippet with safe nesting, e.g. describe
  it in words ("a stray space after the opening parenthesis, before
  `--impacted`") or use a double-backtick-delimited span.

### [nice-to-have] G12 — Report cites "follow-up `#7`" instead of the real issue number
- File: `docs/stories/QS-288.audit-report.md:593-596`
- Severity: nice-to-have
- Source: qs-review-coderabbit (🟡 Minor)
- Description: the release.md banner/test-gap deferral note points at
  "follow-up `#7`" — an internal ordinal, not a resolvable issue
  reference; §6 traceability breaks. Round-1 F16 verified the real
  issue is #295 (its body already carries the amendment).
- Proposed fix: replace "follow-up `#7`" with "follow-up #295"
  (confirm against the §6 table before editing).

### [nice-to-have] G13 — Story pre-judges the #288 mislabel root cause
- File: `docs/stories/QS-288.story.md:27-28`
- Severity: nice-to-have
- Source: qs-review-coderabbit (🟡 Minor)
- Description: the story asserts "#288 itself was mislabeled by
  setup-task", but the audit's settled root cause is the
  `issue-triage.yml` substring matcher, not setup-task — the story
  should carry the original hypothesis neutrally.
- Proposed fix (CodeRabbit's committable suggestion): "…and issue
  `#288` was mislabeled (`area:car/ui/config` on a dev-infra issue);
  the audit later identifies the root cause."

## How to apply

Run `/implement-setup-task` against this fix plan. When done, return
and run `/review-task` again to re-verify.
