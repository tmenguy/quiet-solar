# QS-288 — Review fix plan #04

## Summary
- Source PR: #297
- Source story: docs/stories/QS-288.story.md
- Findings to fix: 8
- Next implement phase: `/implement-setup-task`

Review context (round 4, at PR head `c76076b3`): all round-3 fixes
(H1–H17) verified applied — zero recurrences. All four reviewers
reported, including CodeRabbit (reviewed the head; one open thread =
J5 below). The round-3 ha-battery dispute (H5) was settled
definitively: the current wording is VERIFIED CORRECT against
`ha_model/battery.py` with line-level citations — no change needed
there. Orchestrator doc-maintenance audit clean (`check_doc_drift.py`
exit 0). Zero must-fix findings this round; everything below is a
one-to-three-line edit plus one `gh` thread action.

Triage decision recorded for J5: the user chose to KEEP the tombstone
guard (third consecutive authorization of this deviation: G8, H9/H13,
now J5), record the waiver in the report, and resolve the CodeRabbit
thread — not to revert.

## Findings to fix

### [should-fix] J1 — project-rules.md translations line lacks the #292 qualifier
- File: `docs/workflow/project-rules.md:40-41`
- Severity: should-fix
- Source: qs-review-blind-hunter
- Description: the G2 class survives on a line this PR edited:
  "# Full quality gate (pytest 100% cov + ruff + mypy +
  translations)." sits directly above the edited "# Authoritative
  full-suite gate — enforced in CI on every PR", while CLAUDE.md,
  AGENTS.md, and implement-task.md all gained the "(translations
  value-check is local-full-gate only until #292 lands)" qualifier in
  this same PR.
- Proposed fix: add the same qualifier here, keeping the wording
  consistent with the other three files.

### [should-fix] J2 — adversarial-review.md Claude Code sentence hardcodes "four"
- File: `docs/workflow/adversarial-review.md:29`
- Severity: should-fix
- Source: qs-review-blind-hunter
- Description: "In Claude Code: emit one assistant message with four
  `Agent` tool calls" keeps a hardcoded count immediately above the
  two sentences H15 made count-neutral (Cursor/OpenCode) — wrong for
  the 5-reviewer plan round 2+ case and asymmetric within the same
  paragraph.
- Proposed fix: reword count-neutrally, e.g. "emit one assistant
  message with one `Agent` tool call per reviewer".

### [should-fix] J3 — Merge note's drifted-citation list is incomplete and misdirects
- File: `docs/stories/QS-288.audit-report.md:6-8` (merge note)
- Severity: should-fix
- Source: qs-review-edge-case-hunter + qs-review-blind-hunter
- Description: the enumeration "(D-17, D-18, S-4, S-5, §2)" omits D-9
  and D-14, which also cite `quality_gate.py` lines. Verified: the
  QS-286 merge inserted exactly 208 lines (1752 → 1960); D-9's
  evidence `quality_gate.py:63` now shows `SCRIPT_DIR` instead of the
  cited `SRC_DIR` — and the explicit list tells the reader D-9 is NOT
  stale, actively misdirecting for exactly the case the note (H17)
  was added to prevent.
- Proposed fix: generalize the parenthetical to "all `quality_gate.py`
  line citations in this report resolve against `7d2e5d93` only" (or
  add the two IDs: D-9, D-14).

### [should-fix] J4 — DC1 never names the story-seeded create-plan divergence
- File: `docs/stories/QS-288.audit-report.md:149-153` (DC1)
- Severity: should-fix
- Source: qs-review-acceptance-auditor
- Description: story Method B1 lists two "known seed cases to
  classify (not discard)". The LSP seed is explicitly classified
  (DC2), but the qs-create-plan "Commit and hand off" length
  divergence (~36/~67/~29 lines) is only implicitly covered by DC1's
  class definition and the matrix row `create-plan 62/108/100` — a
  reader cannot verify from the report alone that this seed was
  classified rather than dropped.
- Proposed fix: one-line amendment under DC1, e.g. "incl. the
  story-seeded qs-create-plan 'Commit and hand off' divergence".

### [should-fix] J5 — CodeRabbit: legacy/ frozen-rule conflict — keep guard, record waiver, resolve thread
- File: `docs/stories/QS-288.audit-report.md` (§5 batch F) + `gh`
  thread action on PR #297 (CodeRabbit comment id 3523048692, review
  id 4629409968). Do NOT edit `legacy/scripts/worktree-cleanup.sh`.
- Severity: should-fix
- Source: qs-review-coderabbit (🟠 Major) + qs-review-acceptance-auditor
- Description: CodeRabbit flags that the tombstone guard edit
  conflicts with the "treat `legacy/` as frozen" guideline; its own
  proposed fix allows "explicitly confirm with maintainers that this
  guideline permits a one-time retirement edit". The user has now
  authorized this deviation three times (G8, H9/H13, J5 triage). The
  bundled SC2317 note (unreachable code below the guard) is the
  tombstone working as intended.
- Proposed fix (all three parts):
  1. Add a half-sentence to §5 batch F recording the waiver, e.g.
     "batch F's `git mv` + tombstone guard is a user-waived one-time
     deviation from the `legacy/`-frozen rule and the pure-text
     threshold (confirmed 2026-07-02, reaffirmed at review rounds 2–4)."
  2. Reply to the CodeRabbit thread citing that recorded waiver and
     noting SC2317 is intentional (tombstone), e.g. via
     `gh api repos/tmenguy/quiet-solar/pulls/297/comments/3523048692/replies -f body="…"`.
  3. Resolve the thread (GraphQL `resolveReviewThread`, or leave the
     reply as the record if resolution proves awkward — the reply is
     the substantive part).

### [nice-to-have] J6 — ha-battery.md "attached" bullet blurs probed vs read-on-demand
- File: `docs/agents/concepts/ha-battery.md:58-61` (Key types bullet)
- Severity: nice-to-have
- Source: qs-review-edge-case-hunter
- Description: the bullet lists all five entities under "attached",
  but only the two sensors are probe-attached
  (`battery.py:50-52`); the two numbers and the switch are bare
  entity IDs read on demand via `hass.states.get` and never enter the
  probe/history machinery. The TL;DR draws this distinction; the
  bullet blurs it. A reader could expect `max_charge_number` values
  in `add_to_history` time series.
- Proposed fix: reword, e.g. "HA entities wired: probed —
  `charge_percent_sensor` (SOC), `charge_discharge_sensor` (combined
  power); read on demand — `max_charge_number` +
  `max_discharge_number`, `charge_from_grid_switch` (the only
  switch)." Verify phrasing against `battery.py:50-52,154,198-199,
  230-231` before editing.

### [nice-to-have] J7 — Fix plan #03's no-action closures are untraceable
- File: `docs/stories/QS-288.story_review_fix_#03.md` (preamble, ~line 20)
- Severity: nice-to-have
- Source: qs-review-blind-hunter
- Description: the preamble says "Two findings were closed as
  no-action" but the ID sequence only skips H10 — one visible gap
  cannot account for two closures.
- Proposed fix: name them in the preamble: the gh-metadata
  verification finding (would-have-been H10, closed as already
  externally verified) and the D-16 threshold-stretch note (never
  assigned an ID; record-only).

### [nice-to-have] J8 — Fix plan #02's G11 description still renders a dangling backtick
- File: `docs/stories/QS-288.story_review_fix_#02.md:~141` (G11 description)
- Severity: nice-to-have
- Source: qs-review-blind-hunter
- Description: the span `` `( `--impacted` `` loses the space before
  its closing double-backtick, so the trailing backtick escapes the
  span and renders dangling — in the very finding about broken
  backtick nesting.
- Proposed fix: rewrite the snippet in plain words (as fix plan #01's
  F10 was rewritten), avoiding nested backticks entirely.

## How to apply

Run `/implement-setup-task` against this fix plan. When done, return
and run `/review-task` again to re-verify.
