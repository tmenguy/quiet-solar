# QS-274 — Review fix plan #01

## Summary
- Source PR: #275
- Source story: docs/stories/QS-274.story.md
- Findings to fix: 8 (+1 already addressed directly in the working tree, needs a regression test)
- Next implement phase: `/implement-task`

## Findings to fix

### [must-fix] Add a proper `## Doc maintenance` heading to the PR body
- File: PR #275 body (GitHub — not a repo file)
- Severity: must-fix
- Source: orchestrator doc-maintenance audit
- Description: `python scripts/qs/check_doc_drift.py` exits 1 against the PR's
  changed paths (stale: car-soc-estimation, config-and-setup-flow,
  external-control-detection, load-base, piloted-device-and-heat-pump,
  user-override). The PR body explains these as Doc-OK, but only in a
  `- Doc maintenance:` bullet under `## Summary`, not under a real
  `## Doc maintenance` heading — so the audit/gate does not recognize the
  justification.
- Proposed fix: Edit the PR body (`gh pr edit 275 --body ...`) to promote the
  existing justification into a top-level `## Doc maintenance` heading listing
  each stale-flagged doc and why it is Doc-OK (load.py change is a
  value-literal→constant promotion of the existing "agenda" tag; const.py adds
  additive constants only — no documented behavior changed). Re-run
  `check_doc_drift` to confirm the heading is recognized.

### [should-fix] Cover the `ct is None` fall-through in `get_car_charge_origin_readable_string`
- File: `custom_components/quiet_solar/ha_model/car.py:1169-1177`
- Severity: should-fix
- Source: blind-hunter + edge-case-hunter
- Description: The bare `assert ct is not None` statements were already replaced
  in the working tree with an `if ct is not None:` guard wrapping the
  CALENDAR/MANUAL subcases (every path now returns a string; correct behavior).
  This edit is uncommitted and has no test exercising the `ct is None`
  fall-through for CALENDAR/MANUAL types.
- Proposed fix: Commit the existing edit. Add a regression test in
  `tests/test_car_charge_origin.py` that forces `get_charge_type(...)` to return
  `(CAR_CHARGE_TYPE_CALENDAR, None)` (and `(CAR_CHARGE_TYPE_MANUAL, None)`) and
  asserts the helper falls through to the person / "No proper Forecast"
  branch instead of raising.

### [should-fix] Add the explicit AC2 cleanup-match test (story Task 9)
- File: `tests/ha_tests/test_charger.py` (or `tests/test_load_model.py`, wherever
  the constraint-cleanup path is exercised end-to-end)
- Severity: should-fix
- Source: acceptance-auditor
- Description: AC2 / story Task 9 explicitly required a test that a cleanup call
  passing `load_info={"person": name}` still matches a constraint that also
  carries `{originator: "person"}`. Currently only covered compositionally (the
  dual-key assertion at `test_charger.py:3654-3660` plus separate `_match_ct`
  intersection tests); no single test asserts the AC2 scenario end-to-end.
- Proposed fix: Add one test that builds a person constraint carrying both
  `{"person": name}` and `{CONSTRAINT_ORIGINATOR_KEY: CONSTRAINT_ORIGINATOR_PERSON}`,
  then invokes the cleanup path with `load_info={"person": name}` and asserts the
  constraint is matched/removed.

### [should-fix] Person-forecast test: drive the real method, no mock
- File: `tests/test_car_charge_origin.py:1345,1360`
- Severity: should-fix
- Source: blind-hunter
- Description: The test asserts a string the mocked path does not actually
  produce — it mocks `person.get_forecast_readable_string` but the helper calls
  `self.get_car_person_readable_forecast_mileage()`, so the test can pass for the
  wrong reason / is brittle.
- Proposed fix: Remove the mock. Set up real `current_forecasted_person` /
  forecast state so `get_car_person_readable_forecast_mileage()` runs for real
  and the assertion exercises the actual production path.

### [nice-to-have] Replace hardcoded `"person"` key with a const constant
- File: `custom_components/quiet_solar/ha_model/charger.py:3724,4385`
- Severity: nice-to-have
- Source: CodeRabbit
- Description: The `"person"` load-info key is still hardcoded at the
  constraint-creation/classifier sites even though person constraints are now
  tagged with `CONSTRAINT_ORIGINATOR_PERSON`. Project rule: define all config
  keys in `const.py`, never hardcode strings.
- Proposed fix: Add a constant in `const.py` whose name references "forecasted
  person" (e.g. `LOAD_INFO_FORECASTED_PERSON_KEY = "person"` or
  `CONSTRAINT_FORECASTED_PERSON_KEY`). Replace both literal `"person"` key uses
  with the constant. Keep the wire value `"person"` for back-compat.

### [nice-to-have] AC3 — exercise the real date formatter, not a mock
- File: `tests/test_car_charge_origin.py` (calendar / manual origin tests)
- Severity: nice-to-have
- Source: acceptance-auditor
- Description: The tests mock `get_readable_next_target_date_string` to return a
  fixed `"14:30"` instead of constructing a near-term `end_of_constraint` to
  deterministically assert the real `HH:MM` form, as the story directed.
- Proposed fix: Build a near-term `end_of_constraint` so the real formatter
  returns a deterministic `HH:MM`, and assert the full surrounding string
  (`"Calendar · HH:MM"` / `"Manually set to HH:MM"`) without mocking the
  formatter.

### [nice-to-have] Fix icon-map spacing and the test asserting it
- File: `custom_components/quiet_solar/ui/resources/qs-car-card.js:340`
- Severity: nice-to-have
- Source: blind-hunter
- Description: `"Calendar" : "mdi:calendar"` has a stray space before the colon,
  inconsistent with the other map entries. A smoke test asserts the literal with
  the space, so a later style cleanup would silently break the test.
- Proposed fix: Normalize the spacing to match the other entries
  (`"Calendar": "mdi:calendar"`) and update the corresponding assertion in
  `tests/test_car_card_smoke.py` to match.

### [nice-to-have] Nudge `SENSOR_CAR_CHARGE_ORIGIN` on person reallocation
- File: `custom_components/quiet_solar/ha_model/home.py:2776-2780`
- Severity: nice-to-have
- Source: edge-case-hunter
- Description: `compute_and_set_best_persons_cars_allocations` immediately
  refreshes `SENSOR_CAR_PERSON_FORECAST` on a person-allocation change but not
  the new `SENSOR_CAR_CHARGE_ORIGIN`, which derives from the same state — so the
  origin context row lags until the next `update_states` tick.
- Proposed fix: Add `SENSOR_CAR_CHARGE_ORIGIN` to the same immediate-refresh
  block for parity. Add/extend a test asserting both sensors are nudged.

### [nice-to-have] Handle empty person forecast in the origin line
- File: `custom_components/quiet_solar/ha_model/car.py:1166`
- Severity: nice-to-have
- Source: edge-case-hunter
- Description: When a person-tagged active deadline constraint exists but the
  forecast is empty, the line renders `"Forecasted from Magali: No forecast"`.
  Cosmetic but awkward.
- Proposed fix: Detect the empty-forecast case and render a cleaner string
  (e.g. fall back to a plain "No proper Forecast" or omit the "Forecasted from"
  prefix when there is no mileage/leave-time). Add a test for the empty-forecast
  branch.

## How to apply

Run `/implement-task` against this fix plan. The `get_car_charge_origin_readable_string`
guard edit is already present in the working tree (uncommitted) — commit it as part
of this fix batch and add the regression test described above. When done, return and
run `/review-task` again to re-verify.
