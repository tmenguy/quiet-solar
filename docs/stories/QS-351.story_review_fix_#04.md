# QS-351 — Review fix plan #04

## Summary
- Source PR: #354 (head `29f4fdce`, after review-fix #03)
- Source story: docs/stories/QS-351.story.md
- Previous plans: #01, #02, #03
- Findings to fix: 3 (1 should-fix + 2 nice-to-have) — **all documentation / PR-body only**
- Next implement phase: `implement-task`

Verify-task round 4 roster: `qs-review-edge-case-hunter` (opus, **zero findings**)
+ `qs-review-regression-proof` (opus, 1 should-fix + 2 nice-to-have) +
`qs-review-coderabbit` (still silent/rate-limited — informational, not a
finding). **The code is verified clean:** round-3 SF-1 and N-4 both confirmed
CLOSED by mutation by both full reviewers; no new tie/inversion in either pass;
maxi_val still dominates; FP exact; suites 103 + coverage green; invariant block
arithmetically true for pass 1 and 2; diff-vs-plan clean. No must-fix. This round
is pure documentation cleanup — no production-code change.

## Findings to fix

### [should-fix] SF-1 — PR body top Summary shows stale constant values
- File: PR #354 body ("## Summary" section)
- Severity: should-fix
- Source: qs-review-regression-proof
- Description: The top Summary still states
  "It is now an absolute tie-break PLUGGED_COVERED_CAR_PENALTY_WH=0.5" and
  "threshold is now 1000 Wh and the tie-break 100 Wh". The shipped constants
  (`const.py`) are `PLUGGED_COVERED_CAR_PENALTY_WH = 0.25` and
  `PASS1_PREFERRED_CAR_PENALTY_WH = 0.5` (not 100); the pass-2 offset also now
  carries `PASS2_PREFERRED_CAR_OFFSET_EPS_WH = 1.0`. The correct values appear in
  the lower Review-fix #01/#02/#03 sections, but the Summary is read first — the
  same N-6 staleness class the story fixed, missed in the PR-body Summary.
- Proposed fix: Update the "## Summary" bullets to the shipped values
  (`PLUGGED_COVERED_CAR_PENALTY_WH = 0.25`, `PASS1_PREFERRED_CAR_PENALTY_WH = 0.5`,
  threshold 1000 Wh, and the new pass-2 offset epsilon), so the first thing a
  reader sees matches the merged code. PR-body edit only.

### [nice-to-have] N-1 — Recorded red transcripts abbreviate the sanctioned `-v` form
- File: `docs/stories/QS-351.story.md` (red transcript blocks, ~:787-813) and PR body
- Severity: nice-to-have
- Source: qs-review-regression-proof
- Description: The recorded red blocks drop the `tests/…py::` file-path prefix on
  the node line and omit the trailing `1 failed in …s` summary line — SF-2's own
  remediation text asked for "the `::` invocation line and `1 failed`". Substance
  is verified (the reviewer reproduced all three reds verbatim); form only.
- Proposed fix: Paste the full `-v` node line
  (`tests/test_person_car_allocation.py::…`) and the trailing `1 failed in …s`
  line for the SF-1, N-4 and SF-2 red blocks in the story progress note and PR
  body. Documentation only.

### [nice-to-have] N-2 — SF-2 verbatim block reads in isolation from the round-2 paraphrase
- File: `docs/stories/QS-351.story.md:680-682` (round-2 paraphrase) and the #03
  section (~:806-813, verbatim block)
- Severity: nice-to-have
- Source: qs-review-regression-proof
- Description: SF-2's verbatim block was requested "next to line 680"; it landed
  in the #03 section instead, so the round-2 one-line paraphrase at `:680-682`
  still reads in isolation. History-preserving supersession is acceptable.
- Proposed fix: Add a one-line cross-reference from `:680` to the verbatim block's
  location. Documentation only.

## Housekeeping (thread resolution — not a file edit)

Two CodeRabbit inline threads from the stale 02aaa292 review remain open; since
CodeRabbit will not re-verify, resolve them manually via the GitHub API as part
of this round:
- (a) `const.py:485` "Clamp sub-0.5 Wh charging costs" — the deliberately-skipped
  NTH-E; resolve with a reply noting it was assessed and skipped (harmlessness
  verified by two reviewers; clamping would recreate a tie).
- (c) `docs/stories/QS-351.story.md:498` "Describe PASS1 as a soft preference
  penalty" — the story staleness was addressed across #02/#03/#04; resolve with a
  reply pointing at the shipped `PASS1 = 0.5` wording.

## How to apply

Run `implement-task` against this fix plan. **No production code changes** — this
round edits the PR body and the story progress note only, and resolves the two
CodeRabbit threads. No new tests, no gate impact beyond the doc-drift check
(`## Doc maintenance` heading already present). When done, return and run
`/verify-task` again to re-verify — this should be the final clean pass.
