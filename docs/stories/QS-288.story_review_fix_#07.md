# QS-288 — Review fix plan #07

## Summary
- Source PR: #297
- Source story: docs/stories/QS-288.story.md
- Findings to fix: 6
- Next implement phase: `/implement-setup-task`

Review context (round 7, at PR head `815c482b`): acceptance-auditor
fully clean (all 8 ACs verified, every external claim live-checked);
blind-hunter fully clean on internal consistency (all arithmetic,
ledgers, byte-identical mirror blocks); the L1 supplement rule itself
verified byte-identical across mirrors, enumeration-complete, and
dog-fooded at head (`--quick tests/qs` → 566/566 pass). All 65 prior
findings verified applied — zero recurrences. Zero must-fix. The two
should-fixes below are the gate-supplement rule's last propagation
gaps (authority docs + the product implement agent); M1+M2+M3
complete its propagation tree.

CodeRabbit coverage note: user chose not to wait for CodeRabbit this
round; its last completed review stands at head `ae4d0037` with all 6
review threads resolved (0 open). The deltas since (`815c482b`, and
this plan's apply commit) are doc-only and covered by the three
adversarial reviewers.

No-action, recorded: the #05/#06 generic §5 ledger mapping — an
inherent fixed point per the auditor ("no action needed"); the K1
catch-all covers all later rounds.

## Findings to fix

### [should-fix] M1 — Authority docs still present `--impacted` as the complete pre-commit invariant
- File: `docs/workflow/project-rules.md:58-65,84-95` (gate invariant
  paragraph, ~:88) and `docs/workflow/phase-protocols.md:113-122`
  (implement step 4)
- Severity: should-fix
- Source: qs-review-edge-case-hunter
- Description: five non-authority files (the implement-setup-task
  command, its three mirrors, AGENTS.md, CLAUDE.md) now mandate the
  `--quick tests/qs` supplement, but the process-authority docs say
  "The implement phase ALWAYS runs `--impacted` before commit/PR"
  with `--quick` framed as an ad-hoc convenience. The story's own
  canonical-source rule makes `docs/workflow/*` the winner in mirror
  re-syncs — so follow-up #289's back-port (or any future
  harness-sync pass) could reconcile the supplement away as
  undeclared drift.
- Proposed fix: add one sentence to project-rules.md's invariant
  paragraph and one clause to phase-protocols.md's implement step 4,
  reusing the L1 trigger phrasing verbatim: "For change sets touching
  any `tests/qs`-pinned non-Python file (agent files, commands,
  workflow docs, `.claude/settings.json`) — even when Python files
  changed too — also run `python scripts/qs/quality_gate.py --quick
  tests/qs` before commit."

### [should-fix] M2 — qs-implement-task (product variant) lacks the supplement
- File: `.claude/agents/qs-implement-task.md:92-108` (section 4) +
  mirrors `.cursor/agents/qs-implement-task.md` /
  `.opencode/agents/qs-implement-task.md` (three-mirror co-edit rule
  applies), and `.claude/commands/implement-task.md:22-25` (expected
  outcome)
- Severity: should-fix
- Source: qs-review-edge-case-hunter
- Description: the routing rule selects implement-setup-task only
  when ALL touched files are dev-env; any change set mixing
  `custom_components/` code with a pinned non-Python file (e.g.
  `.claude/settings.json` allowlisting a new command, or a workflow
  doc) routes to qs-implement-task — whose gate section carries no
  supplement and whose hard rules leave those pinned files editable.
  `--impacted` then selects product tests only and a broken pin
  (e.g. `tests/qs/agents/test_lsp_tool_enabled.py`) first fails in
  CI — the exact window K3/L1 closed, one router up.
- Proposed fix: add the same supplement block (byte-identical
  wording, adapted heading level if needed) to qs-implement-task's
  section 4 in all three mirrors, and the matching bullet to
  `implement-task.md`'s expected outcome. Keep the block text
  identical to the qs-implement-setup-task one so future syncs treat
  them as one unit.

### [nice-to-have] M3 — AGENTS.md supplement mentions drop the enumeration
- File: `AGENTS.md:31` (commands block comment) and `:84-86`
  (Quality-gate paragraph)
- Severity: nice-to-have
- Source: qs-review-edge-case-hunter
- Description: both AGENTS.md statements omit the "(agent files,
  commands, workflow docs, `.claude/settings.json`)" enumeration that
  CLAUDE.md and the four contract files carry — and AGENTS.md is the
  sole steering doc for Codex (no agent-mirror directory), where
  "tests/qs-pinned" is otherwise undiscoverable without grepping.
- Proposed fix: append the parenthetical to the `--quick tests/qs`
  comment at :31 (or to the :84-86 paragraph if line length is a
  concern), matching the L1 wording.

### [nice-to-have] M4 — Fix plan #01's F2 description: backtick-nesting class, third occurrence
- File: `docs/stories/QS-288.story_review_fix_#01.md:34` (F2 description)
- Severity: nice-to-have
- Source: qs-review-blind-hunter
- Description: the span opening at `"Cold" =\`` uses a backslash that
  does not escape inside a CommonMark code span, so the single
  backtick only closes at the tick before `.testmondata` two lines
  down — garbling F2's text. Same class as the G11/J8 fixes.
- Proposed fix: rewrite with a double-backtick span or plain words
  (as done for F10/G11), avoiding nested backticks.

### [nice-to-have] M5 — §6's #289 row doesn't record the content-checker fold
- File: `docs/stories/QS-288.audit-report.md:~646` (§6 row for #289)
- Severity: nice-to-have
- Source: qs-review-blind-hunter
- Description: `docs/workflow/harness.md:148` says the
  `lint_agents.py` content-checker was "folded into follow-up #289"
  (H12; the live issue body carries it), but the report's §6 ledger
  row for #289 still lists only "D-2, DC5, S-9" — the fold isn't
  traceable from the in-repo ledger.
- Proposed fix: extend the #289 row's finding list with the
  content-checker fold, e.g. "D-2, DC5, S-9 (+ `lint_agents.py`
  content-checker, folded in at review round 3)".

### [nice-to-have] M6 — §2 blanket "N=5" vs the two N=3 sub-timing rows
- File: `docs/stories/QS-288.audit-report.md:81-82` (§2 protocol
  header) vs `:89-90` (supplementary `gh` sub-timing rows)
- Severity: nice-to-have
- Source: qs-review-acceptance-auditor
- Description: the header states "N=5 warm repeats" as the blanket
  protocol while the two supplementary `gh` sub-timing rows are N=3
  (self-annotated). Neither row is among AC2's six required timings,
  so no AC impact — a one-clause caveat makes §2 airtight.
- Proposed fix: add "(supplementary sub-timings N=3)" to the header
  sentence.

## How to apply

Run `/implement-setup-task` against this fix plan. When done, return
and run `/review-task` again to re-verify.
