# QS-288 — Review fix plan #01

## Summary
- Source PR: #297
- Source story: docs/stories/QS-288.story.md
- Findings to fix: 16
- Next implement phase: `/implement-setup-task`

Review context: findings come from qs-review-blind-hunter,
qs-review-edge-case-hunter, and qs-review-acceptance-auditor.
qs-review-coderabbit was rate-limited and produced no review (may be
re-triggered after ≈23:40 UTC). Orchestrator doc-maintenance audit was
clean (`check_doc_drift.py` exit 0 on the PR's changed set).

## Findings to fix

### [must-fix] F1 — D-21/D-22 break the universal finding schema (AC5)
- File: `docs/stories/QS-288.audit-report.md:410` and `:413`
- Severity: must-fix
- Source: qs-review-acceptance-auditor
- Description: AC5 requires every D-finding to carry ID, severity,
  evidence, proposal, effort, disposition. D-21 has no `Effort` field
  (proposal is only "note only"); D-22 has neither `Effort` nor an
  explicit `Proposed:` disposition ("No action" is informal). The
  report's own convention shows wontfix entries carry both (D-20,
  line 404: "Effort S. Proposed: wontfix").
- Proposed fix: append `Effort S. **Proposed: wontfix.**` to D-21 and
  `Effort —. **Proposed: no action (clean result).**` to D-22.

### [should-fix] F2 — Stray backtick garbles story Markdown
- File: `docs/stories/QS-288.story.md:100`
- Severity: should-fix
- Source: qs-review-blind-hunter
- Description: `"Cold" =\`` opens an inline code span that only closes
  at the backtick before `.testmondata` on line 102 — lines 100–102
  render as a garbled code span and `.testmondata` loses its
  formatting.
- Proposed fix: delete the stray backtick so the line reads
  `"Cold" = first invocation …` (matching audit report §2 wording).

### [should-fix] F3 — ha-battery.md collapses probe tri-state to bool
- File: `docs/agents/concepts/ha-battery.md:41`
- Severity: should-fix
- Source: qs-review-edge-case-hunter
- Description: the rewritten Core-idea paragraph says
  `probe_if_command_set()` returns "a plain bool", but the source is
  `-> bool | None` with three explicit `return None` paths
  (`custom_components/quiet_solar/ha_model/battery.py:100-119`), hit
  when the charge-from-grid switch or a max-power number state can't
  be read (HA cold start, inverter offline, entity unconfigured). The
  D-11 accuracy rewrite itself collapses `None` (inconclusive) into
  `False` (command not in effect).
- Proposed fix: document the tri-state, e.g. "(returns `bool | None` —
  `None` when a required entity state can't be read; …)". Verify
  wording against `battery.py` before editing.

### [should-fix] F4 — harness.md dangling forward reference to an unfiled follow-up
- File: `docs/workflow/harness.md:148`
- Severity: should-fix
- Source: qs-review-edge-case-hunter
- Description: new text says `scripts/qs/lint_agents.py` is "not yet
  built; **proposed as a QS-288 follow-up**", but none of the eight
  filed follow-ups (#289–#296) covers a content-level sync checker
  (#289 is only the back-port re-sync). A reader chasing the reference
  via `gh issue list` or report §6 finds nothing — the same staleness
  class D-1 was fixing.
- Proposed fix: soften to "a candidate follow-up (not yet filed)".
  Alternative (if preferred while editing): append a sentence to
  #289's body covering the content-level checker and keep the
  cross-reference concrete — pick one, don't do both.

### [should-fix] F5 — AC7 confirmation record missing; D-16 disposition left open
- File: `docs/stories/QS-288.audit-report.md:545-559` (§5) and `:341` (D-16)
- Severity: should-fix
- Source: qs-review-acceptance-auditor
- Description: AC7's "user-confirmed list … before any fix commit" is
  not verifiable from the artifact. §6 records user acceptance for the
  follow-up issues and the relabel, but §5 carries no equivalent
  record for the inline-fix batches, and D-16's disposition still
  reads forward-looking "user to confirm" even though its commit
  (203d7bf1) landed. (D-16 also stretches the "pure text/doc" inline
  threshold — script `git mv` + `settings.json` allowlist trim —
  making the recorded confirmation matter more.)
- Proposed fix: add one line to §5 — "Inline-fix list D-1, D-3–D-9,
  D-11, D-16, D-19 user-confirmed <date> before batch A" — and close
  D-16's parenthetical to reflect the confirmation and landed commit.

### [should-fix] F6 — AC4: release.py verdict and Method C1 call-structure result missing
- File: `docs/stories/QS-288.audit-report.md` (§3, §4, and "not audited" list at `:436`)
- Severity: should-fix
- Source: qs-review-acceptance-auditor
- Description: AC4 names `release.py` explicitly, but §3 contains
  neither a finding nor an explicit no-findings statement for it (only
  an incidental mention inside D-17, line 385). Method C1's mandated
  "static call-structure analysis of `release.py` and `create_pr.py`
  (their gh/network call chains)" appears nowhere in §4 and is not on
  the "not audited" list.
- Proposed fix: do the small static read of `scripts/qs/release.py`
  and `scripts/qs/create_pr.py` call chains and record the result in
  §3/§4 (e.g. "read; no findings beyond D-17/D-20 context"); if not
  done, honestly add both to the "not audited" list instead. One of
  the two — the report must not stay silent.

### [nice-to-have] F7 — add_to_history shown with two different signatures
- File: `docs/agents/concepts/ha-device-mixin.md:40` (vs `:59`)
- Severity: nice-to-have
- Source: qs-review-blind-hunter
- Description: line 40 shows `add_to_history(entity_id, time, state)`
  while line 59 shows `add_to_history(entity_id, time=None,
  state=None, ...)` — the same function with two shapes, in a doc
  whose whole point (D-11) was fixing fabricated signatures.
- Proposed fix: verify the actual signature in source (edge-case
  hunter confirmed line 59's form matches `device.py`) and harmonize
  both mentions on the source-accurate form.

### [nice-to-have] F8 — Arithmetic display self-consistency in S-scoring
- File: `docs/stories/QS-288.audit-report.md:504`
- Severity: nice-to-have
- Source: qs-review-blind-hunter
- Description: `3.1 × 12 ÷ 3 = 12.3` is actually 12.4; the 12.3 comes
  from the measured 3.07s. AC6 requires the arithmetic shown to be
  self-consistent.
- Proposed fix: write `3.07 × 12 ÷ 3 ≈ 12.3`.

### [nice-to-have] F9 — AC2 wording contradicts Method A's full-gate N=1 pin
- File: `docs/stories/QS-288.story.md:251`
- Severity: nice-to-have
- Source: qs-review-blind-hunter
- Description: AC2's "each with exact command line, N=5 median +
  min/max" literally includes the full-gate reference, but Method A
  (line 117) and report §2 record the full gate as a sanctioned N=1
  one-off.
- Proposed fix: reword AC2 to exempt the full-gate reference from the
  N=5 requirement (e.g. "…N=5 median + min/max (full-gate reference:
  N=1, per Method A)").

### [nice-to-have] F10 — Stray space inside parenthesis
- File: `docs/stories/QS-288.audit-report.md:493`
- Severity: nice-to-have
- Source: qs-review-blind-hunter
- Description: cosmetic: a stray space after the opening parenthesis,
  before `--impacted`.
- Proposed fix: remove the space.

### [nice-to-have] F11 — CLAUDE.md quality-gate section predates --impacted doctrine
- File: `CLAUDE.md` ("Quality gate" section)
- Severity: nice-to-have
- Source: qs-review-blind-hunter
- Description: not touched by the PR — it still documents only the
  bare full gate (`python scripts/qs/quality_gate.py`) while
  AGENTS.md/project-rules.md now mandate `--impacted` pre-commit. May
  have been intentionally deferred to the startup-reading-dedup
  follow-up; fixing it now closes the drift.
- Proposed fix: add one sentence to the "Quality gate" section noting
  the pre-commit form is `python scripts/qs/quality_gate.py
  --impacted` and the whole-suite gate runs authoritatively in CI —
  mirror the wording already landed in AGENTS.md.

### [nice-to-have] F12 — D-16 completeness claim overstates reference scan
- File: `docs/stories/QS-288.audit-report.md:334`
- Severity: nice-to-have
- Source: qs-review-edge-case-hunter
- Description: "repo-wide references: only its own usage line and two
  `.claude/settings.json:36-37` allowlist entries" is false as
  written — `docs/stories/QS-173.story.md` and two `QS-legacy-*`
  stories also reference `scripts/worktree-cleanup.sh` (~15 grep hits
  vs the claimed 3; frozen artifacts, so nothing breaks).
- Proposed fix: qualify as "live references (frozen `docs/stories/`
  and `legacy/` excluded)".

### [nice-to-have] F13 — Retired script still executable with no retirement marker
- File: `legacy/scripts/worktree-cleanup.sh`
- Severity: nice-to-have
- Source: qs-review-edge-case-hunter
- Description: the script remains executable and fully functional from
  its new location (`MAIN_DIR` resolution is position-independent),
  with a stale self-referencing usage line and no retirement marker.
  Running `bash legacy/scripts/worktree-cleanup.sh <N>` force-removes
  a worktree and deletes the local branch — behavior the live
  `scripts/qs/cleanup_worktree.py` deliberately dropped. `legacy/` is
  declared-frozen, so keep the change minimal.
- Proposed fix: `chmod -x` the file and add a short retirement header
  comment ("RETIRED (QS-288 D-16) — superseded by
  `scripts/qs/cleanup_worktree.py`; do not run"). Verify the
  legacy-quarantine test still passes (it scans only opencode agent
  bodies, so this should be safe).

### [nice-to-have] F14 — §5 ledger cadence deviation unrecorded
- File: `docs/stories/QS-288.audit-report.md` (§5)
- Severity: nice-to-have
- Source: qs-review-acceptance-auditor
- Description: story T7 mandates appending `D-n → <short-hash>` to §5
  after each batch commit; the branch instead carries one consolidated
  ledger commit (4f4bddf0) after all six batches. End state satisfies
  the AC letter; the cadence deviation deserves an honesty note.
- Proposed fix: add one line to §5 noting the ledger was appended as a
  single consolidated commit after all six batches.

### [nice-to-have] F15 — Spurious auto-labels on the new follow-up issues
- File: n/a (GitHub issue metadata — `gh` actions only)
- Severity: nice-to-have
- Source: qs-review-acceptance-auditor
- Description: seven of the eight follow-ups (#289, #290, #292–#296)
  already carry spurious `area:car`/`area:ui`/`area:config` labels
  auto-added by the buggy `issue-triage.yml` substring matcher — the
  exact D-10 bug re-demonstrating itself on its own follow-ups (#291
  is clean).
- Proposed fix: for each affected issue, check its labels
  (`gh issue view <n> --json labels`) and strip the spurious `area:*`
  labels (`gh issue edit <n> --remove-label …`), keeping
  `area:dev-pipeline`. Optionally add a one-line comment on #293
  noting the re-demonstration.

### [nice-to-have] F16 — Ensure #295 covers the release.md preamble-test gap fully
- File: n/a (GitHub issue metadata; context: `tests/qs/agents/test_slash_command_preambles.py`)
- Severity: nice-to-have
- Source: qs-review-acceptance-auditor
- Description: after D-4 added the banner to
  `.claude/commands/release.md`, the preamble test still parametrizes
  only the six chained commands and carries a now-false "release.md is
  intentionally excluded" comment. §5 explicitly ledgers this deferral
  to follow-up #295 — accepted; just make sure #295's body names both
  the stale comment and the missing parametrization.
- Proposed fix: read #295 (`gh issue view 295`); if either item is
  missing from its body, amend the body (`gh issue edit 295 --body …`)
  to name both. No repo edit in this PR.

## How to apply

Run `/implement-setup-task` against this fix plan. When done, return
and run `/review-task` again to re-verify.
