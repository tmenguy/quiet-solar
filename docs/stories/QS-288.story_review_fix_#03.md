# QS-288 — Review fix plan #03

## Summary
- Source PR: #297
- Source story: docs/stories/QS-288.story.md
- Findings to fix: 16
- Next implement phase: `/implement-setup-task`

Review context (post-merge round, at PR head `e23ae789` = merge of
origin/main bringing in QS-286): all round-1 (F1–F16) and round-2
(G1–G13) fixes verified applied and surviving the merge — zero
recurrences. All four reviewers reported; CodeRabbit reviewed the
merged head with zero new findings and all prior threads resolved.
Orchestrator doc-maintenance audit clean (`check_doc_drift.py` exit 0
at merged head). H3/H4 are merge-boundary artifacts (main moved under
the PR); H1 is the round's one substantive content finding.

Two findings were closed as no-action during triage (not listed
below): the fix plans' gh-metadata promises (already externally
verified: labels, #295 body, PR-body count phrase) and the D-16
threshold-stretch note (record-only; confirmation already recorded).

## Findings to fix

### [should-fix] H1 — S-2's CI-cost estimate contradicted by measured CI history
- File: `docs/stories/QS-288.audit-report.md:477-479` (S-2), `:28-38`
  (§1 top-3), plus a `gh` comment on issue #292
- Severity: should-fix
- Source: qs-review-acceptance-auditor
- Description: S-2 estimates the CI suite at "plausibly 20–30 min"
  with "~900s saved per blocking wait" (score ≈ 200, ranked #1). The
  repo's own CI history says otherwise: the `pr-quality.yml`
  "Tests (100% Coverage)" job completes in ~3m44s–5m01s across the
  last four runs (e.g. this PR's head run = 4m38s). A ~4.5-min job
  cannot yield 900s of savings; plausible saving from 4-way sharding
  is ~180s → score ≈ `180 × 2 ÷ 9 = 40`, demoting S-2 from #1 to #3.
  S-2 self-declares "not measured here — CI-side; assumption-
  sensitive", so AC6's letter is met — but the ranking is distorted.
- Proposed fix: amend S-2's cost/saving/score with the measured CI
  job times (`gh run list --workflow pr-quality.yml` for fresh
  evidence), re-sort §4 (S-2 moves below S-3 and S-1), update §1's
  top-3 ordering (S-3 leads), and post a short corrective comment on
  follow-up #292 noting the corrected baseline (it was filed on the
  stale premise).

### [should-fix] H2 — D-21 is the only finding without an explicit `Proposal:` clause
- File: `docs/stories/QS-288.audit-report.md:414-416` (D-21)
- Severity: should-fix
- Source: qs-review-blind-hunter + qs-review-acceptance-auditor
  (flagged independently in three reviewer passes)
- Description: AC5 mandates ID/severity/evidence/proposal/effort/
  disposition per D-finding. D-21's proposal is only implicit in
  "wontfix (note only)"; every other finding, including fellow-wontfix
  D-20, carries the field.
- Proposed fix: add `Proposal: none (leave as-is).` (or equivalent)
  to D-21 — two-word amendment.

### [should-fix] H3 — AGENTS.md "full grammar" lacks QS-286's `--seed-testmon-status`
- File: `AGENTS.md:29-37` (commands block)
- Severity: should-fix
- Source: qs-review-edge-case-hunter
- Description: merge-boundary recurrence of the PR's own D-19 class.
  The PR rewrites this block as the entry-point grammar (line 84
  points to it as "the full grammar"), but merged main (QS-286) added
  `--seed-testmon-status`, now documented in the parallel
  `docs/workflow/project-rules.md:73-76` block and actively
  recommended by `.opencode/agents/qs-finish-task.md:193`. The two
  command blocks disagree.
- Proposed fix: add one line mirroring project-rules.md, e.g.
  `python scripts/qs/quality_gate.py --seed-testmon-status  # read-only: report the detached --seed-testmon run's completion (0 = ok, 4 = running, 1 = rerun, 3 = no status)`
  Keep the wording consistent with project-rules.md.

### [should-fix] H4 — implement-setup-task.md frontmatter still advertises the dev-only fast path
- File: `.claude/commands/implement-setup-task.md:2` (frontmatter `description:`)
- Severity: should-fix
- Source: qs-review-edge-case-hunter
- Description: asymmetric D-3 fix. The sibling `implement-task.md:2`
  description was updated to "pass the impacted quality gate
  (--impacted)" and this file's body bullet mandates `--impacted`,
  but this frontmatter still reads "Quality gate runs in dev-only
  fast path" — the exact contradiction class D-3 eliminated, one line
  above the fix.
- Proposed fix: reword to match the sibling, e.g. "…dev-environment
  paths…; pass the impacted quality gate (--impacted;
  coverage-vacuous on dev-only trees), open PR."

### [should-fix] H5 — ha-battery.md `None`-semantics: unconfigured entities never yield `None`
- File: `docs/agents/concepts/ha-battery.md:42-44`
- Severity: should-fix
- Source: qs-review-edge-case-hunter (pre-merge pass, detailed source
  citations; the post-merge pass summarily said "verified" — VERIFY
  FIRST, see below)
- Description: the tri-state parenthetical claims `None` means "a
  required entity state could not be read (HA cold start, inverter
  offline, unconfigured entity)". Per the detailed source walk:
  `_command_to_values` nulls the *expected* value for any unconfigured
  entity (`custom_components/quiet_solar/ha_model/battery.py:77-84`),
  and `probe_if_command_set` returns `None` only when the expected
  value is non-None and the read is None (`:105,111,117`);
  unconfigured entities are excluded and vacuously match (`:121-125`)
  — a fully-unconfigured battery returns `True`, not `None`.
- Proposed fix: FIRST re-verify against `battery.py:77-125`. If
  confirmed (expected), drop "unconfigured entity" from the `None`
  parenthetical and state the real behavior, e.g. "(HA cold start,
  inverter offline — i.e. a *configured* entity whose state is
  unreadable); unconfigured entities are skipped and treated as
  vacuously matching." If NOT confirmed, record the false positive in
  the commit message and leave the text.

### [nice-to-have] H6 — `gh --json` field list wraps inside a code span
- File: `docs/stories/QS-288.audit-report.md` (§3, D-2 item 3)
- Severity: nice-to-have
- Source: qs-review-blind-hunter
- Description: the inline code span
  `--json state,mergeable,mergedAt,title,` … `url,headRefName,…`
  wraps across a line break; CommonMark renders the newline as a
  space, so copy-pasting the flag yields an invalid `gh` command.
- Proposed fix: keep the whole span on one line (long line acceptable
  in the report).

### [nice-to-have] H7 — Stage-attribution arithmetic wobble (9.7 vs 9.81; 10.55 vs 10.66)
- File: `docs/stories/QS-288.audit-report.md:105` (stage table) and
  `:558-567` (S-10 split)
- Severity: nice-to-have
- Source: qs-review-blind-hunter (both rounds)
- Description: the pytest stage window 0.69–10.50s spans 9.81s but is
  headlined "**9.7s** for 0 selected tests" (9.7 = 3.07 + ~6.6
  sub-parts, ~0.1s unattributed); S-10 reuses a "0.69 / 9.7 / 0.16
  split" summing 10.55 vs the 10.66 end mark. In a report whose ACs
  demand shown arithmetic, the wobble reads as an error.
- Proposed fix: write "~9.8s" (or add tildes) in both places and
  reconcile the S-10 split against the recorded end mark.

### [nice-to-have] H8 — "the other five delegate faithfully" is a miscount
- File: `docs/stories/QS-288.audit-report.md:429-431`
- Severity: nice-to-have
- Source: qs-review-blind-hunter
- Description: D-3 covers 2 files and D-4 covers 1, so 4 of the 7
  command files are finding-free — "five" only works if release.md
  (D-4, banner nit) still counts as delegating faithfully.
- Proposed fix: say "the other four" (or add the release.md
  qualifier).

### [nice-to-have] H9 — Tombstone guard: bare `exit 1` kills a sourcing shell
- File: `legacy/scripts/worktree-cleanup.sh:5` (guard line)
- Severity: nice-to-have
- Source: qs-review-blind-hunter
- Description: if the file is ever `source`d, the bare `exit 1`
  terminates the caller's shell.
- Proposed fix: use the defensive idiom
  `return 1 2>/dev/null || exit 1`. Combine with H13 (same file, same
  edit).

### [nice-to-have] H11 — §6 lacks the Design's "body draft per proposal"
- File: `docs/stories/QS-288.audit-report.md:610-624` (§6)
- Severity: nice-to-have
- Source: qs-review-acceptance-auditor
- Description: the story's Design defines §6 as "title + body draft
  per proposal; URL for each user-accepted one"; §6 delivers titles +
  URLs only. Materially moot (all 8 proposals were accepted and
  filed; live bodies supersede drafts) but a pedantic read notices.
- Proposed fix: one sentence in §6, e.g. "Body drafts were superseded
  by the filed issues themselves — each body (authored at filing)
  links back to this report."

### [nice-to-have] H12 — Make the `lint_agents.py` content-checker decision durable
- File: `docs/workflow/harness.md:148` (parenthetical) and issue #289
  (gh action); optionally `docs/stories/QS-288.audit-report.md` §3 D-1
- Severity: nice-to-have
- Source: qs-review-acceptance-auditor
- Description: harness.md's "a candidate follow-up (not yet filed)"
  is currently truthful but will rot; D-1's proposal mentions tying
  the note to a content-checker follow-up that was never surfaced as
  a disposition.
- Proposed fix (primary): fold the content-level sync checker into
  #289's scope (`gh issue edit 289 --body …` appending one sentence —
  the re-sync is its natural first use case), then update the
  harness.md parenthetical to cite #289 concretely. Alternative: add
  an explicit deferred/wontfix disposition line to §3 D-1 and leave
  harness.md as-is. Pick one.

### [nice-to-have] H13 — Tombstone exec-bit UX: direct execution loses the redirect message
- File: `legacy/scripts/worktree-cleanup.sh:1-6`
- Severity: nice-to-have
- Source: qs-review-edge-case-hunter
- Description: with the exec bit stripped, `./legacy/scripts/…` yields
  the shell's bare "permission denied" and the line-5 pointer to
  `scripts/qs/cleanup_worktree.py` never prints; the redirect is only
  visible via `bash …` or opening the file.
- Proposed fix: extend the header comment to say the file is
  intentionally non-executable (keeps the guard + non-exec posture;
  avoids re-blessing execution). Same edit as H9.

### [nice-to-have] H14 — Rounded scores displayed with `=` instead of `≈`
- File: `docs/stories/QS-288.audit-report.md:505,516,549`
- Severity: nice-to-have
- Source: qs-review-blind-hunter
- Description: `= 69.3`, `= 16.8`, `= 1.3` for true 69.33/16.83/1.32,
  while round-1 F8 set the `≈` precedent on S-5.
- Proposed fix: switch those three to `≈`. (Coordinate with H1's
  re-sort — line numbers will shift.)

### [nice-to-have] H15 — adversarial-review.md Cursor sentence hardcodes "four subagents"
- File: `docs/workflow/adversarial-review.md:32`
- Severity: nice-to-have
- Source: qs-review-blind-hunter
- Description: the unchanged Cursor sentence still reads "invokes the
  four subagents" directly above the PR's new count-neutral OpenCode
  wording ("all of the round's reviewers") — stale for the
  five-reviewer round-2+ plan case (delta-auditor).
- Proposed fix: make the Cursor sentence count-neutral to match, e.g.
  "invokes the round's reviewer subagents".

### [nice-to-have] H16 — §5 pins fix-plan #02's apply commit as a self-reference
- File: `docs/stories/QS-288.audit-report.md:599-600` (§5)
- Severity: nice-to-have
- Source: qs-review-blind-hunter + qs-review-acceptance-auditor
- Description: §5 ledgers fix-plan #02 as "the commit that carries
  this line (see PR #297 history)" — permanently unresolvable in a
  ledger that otherwise pins short SHAs. The real SHA is `acb1cc80`.
- Proposed fix: replace the self-reference with `acb1cc80`, and add
  the fix-plan #03 row for this round's apply commit using the same
  rolling-cadence convention (its SHA rides the next amendment or is
  noted as "this commit").

### [nice-to-have] H17 — Note that post-audit merge shifted quality_gate.py line anchors
- File: `docs/stories/QS-288.audit-report.md:3-4` (header) or §2 env block
- Severity: nice-to-have
- Source: qs-review-edge-case-hunter + qs-review-acceptance-auditor
- Description: the QS-286 merge inserted ~208 lines into
  `scripts/qs/quality_gate.py`, so the report's line citations for
  D-17/D-18/S-4/S-5 no longer resolve at PR HEAD (e.g. the cited
  `--impacted` short-circuit `:1644` is now ~`:1852`). The
  "Tree audited: `7d2e5d93` (clean)" pin technically covers it, but
  nothing warns the reader. Behavioral claims all still hold
  (re-verified against the merged source).
- Proposed fix: one sentence in the header or §2: "origin/main
  (QS-286) was merged post-audit (`e23ae789`); `quality_gate.py` line
  citations resolve against `7d2e5d93` only."

## How to apply

Run `/implement-setup-task` against this fix plan. When done, return
and run `/review-task` again to re-verify.
