# QS-372 — Review fix plan #04

## Summary
- Source PR: #374 (re-review round 4, head 41aa0950)
- Source story: docs/stories/QS-372.story.md
- Findings to fix: 6 (2 should-fix, 4 nice-to-have)
- Next implement phase: `/implement-setup-task`
- Triage: done autonomously by the review orchestrator (the user delegated
  the implement ↔ review loop). Round 4: no must-fix; acceptance auditor
  clean (AC1–AC7 + all fix #01–#03 items OK; CI fully green at 41aa0950).
  CodeRabbit still rate-limited. Findings are converging to prose polish
  of the `implement_routing` wait loop.

## Findings to fix

### [should-fix] S1 — lookup-failure STOP invites "retry" but the wait loop doesn't handle it
- File: `scripts/qs/agent_templates/_macros.md.j2:23-29, 57-61` (`implement_routing`),
  `tests/qs/agents/test_handoff_macro.py` (`test_implement_variant_routes_by_declared_target`)
- Source: qs-review-blind-hunter + qs-review-edge-case-hunter + qs-review-acceptance-auditor
- Description: the lookup-failure message says "retry, or tell me which
  variant to run", then "Do **not** print the label message or commands
  below" — ambiguous whether the wait paragraph (below) applies; and the
  loop does not recognise "retry" ("any other answer → ask again").
- Proposed fix:
  - ":29" → "Do **not** print the label message or commands below; then
    wait for the user as described in the last paragraph."
  - Wait paragraph → "After printing (either STOP), wait for the user: if
    they say to retry or report adding a label, re-run `context.py` and
    route again; if they name …".
  - Pin "(either STOP)" and "retry" in the AC4 test.

### [should-fix] S2 — a slash-prefixed answer is assigned verbatim
- File: `scripts/qs/agent_templates/_macros.md.j2:57-61`
- Source: qs-review-edge-case-hunter
- Description: the loop accepts `/implement-task` but says only "use it";
  `{{next_implement}} = /implement-task` then renders `//implement-task` in
  the Claude fix plan and, on OpenCode, makes the spawn success check
  expect `qs-/implement-task` (false STOP, orphaned session).
- Proposed fix: "use it (assign the bare name — drop any leading `/`)".
  Pin the phrase in the AC4 test.

### [nice-to-have] N1 — lookup-failure message blames the issue lookup only
- File: `scripts/qs/agent_templates/_macros.md.j2:~26`, test literal at `test_handoff_macro.py:~293`
- Source: qs-review-edge-case-hunter
- Proposed fix: message → `Cannot pick the implement variant: the context.py / gh lookup failed — retry, or tell me which variant to run.`
  (stable prefix kept). Add one prose clause after it: "(If `issue` is
  null — the branch carries no `QS_<N>` issue number — say so instead of
  suggesting a retry.)" Update the pinned literal and the story's
  Code-review note.

### [nice-to-have] N2 — test golden / docstrings lag the N4a blank-line rule
- Files: `tests/qs/launchers/test_claude_launcher.py:~2011, ~2023`,
  `scripts/qs/launchers/claude.py:~613`
- Source: qs-review-blind-hunter
- Proposed fix: golden builds the prompt with `f"  {line}" if line else ""`;
  both docstrings say "every non-empty prompt line indented two spaces;
  blank lines stay empty".

### [nice-to-have] N3 — docs consistency
- Files: `docs/workflow/harness.md:~308-310`, `docs/workflow/overview.md:~161-163`
- Source: qs-review-blind-hunter + qs-review-acceptance-auditor
- Proposed fix: (a) harness.md — rewrap to remove the orphan `nothing`
  line ("… stderr warning naming the file. **The skip is terminal, not" /
  "transient:** nothing repairs the file, so every later handoff in that" /
  "worktree refuses it too."). (b) overview.md — "An empty target (no or
  ambiguous label) STOPs the orchestrator, which asks the user to label the
  issue; a failed `context.py` / `gh` lookup STOPs first with its own
  message." Keep AC5 phrases absent.

### [nice-to-have] N4 — story §4/§5 design text superseded
- File: `docs/stories/QS-372.story.md:~364-372` (§4 blockquote), `:~399-402` (§5 harness.md bullet)
- Source: qs-review-acceptance-auditor
- Proposed fix: add a one-line "(superseded by Code review #01–#04 — see
  Adversarial Review Notes)" marker above the §4 blockquote and after the
  §5 harness.md bullet; add a short "Code review #04" note (S1/S2/N1).

## Skipped (triaged out)
- Fix plan #01–#03 "Skipped" items remain skipped.

## How to apply

Run `/implement-setup-task` against this fix plan. When done, return and
run `/review-task` again to re-verify.
