# QS-288 — Review fix plan #05

## Summary
- Source PR: #297
- Source story: docs/stories/QS-288.story.md
- Findings to fix: 7
- Next implement phase: `/implement-setup-task`

Review context (round 5, at PR head `933c891a`): all round-4 fixes
(J1–J8) verified applied — zero recurrences. All four reviewers
reported. CodeRabbit reviewed the head clean (0 findings above
nice-to-have) and formally WITHDREW the legacy-frozen concern (thread
3523048692 resolved: "explicitly maintainer-authorized, one-time
deviation"). The tombstone guard was empirically tested in every
invocation mode (bash/zsh/sh exec, source, `./` without x-bit, stderr
closed) — all safe. Orchestrator doc-maintenance audit clean
(`check_doc_drift.py` exit 0). Zero must-fix. K3 is the round's one
substantive finding; the rest are one-liners.

Dropped as no-action (recorded here for traceability): the
`context.py` cold-timing caveat — already honestly disclosed in the
report (auditor: "no action needed beyond keeping the disclosure").

## Findings to fix

### [should-fix] K1 — §5 review-round ledger: close it out and end the recursion
- File: `docs/stories/QS-288.audit-report.md:621-624` (§5 "Review-round fixes")
- Severity: should-fix
- Source: qs-review-acceptance-auditor + qs-review-blind-hunter +
  qs-review-edge-case-hunter (three-reviewer consensus)
- Description: the ledger maps #01 → `b8ce78be`, #02 → `acb1cc80`,
  #03 → "the commit that carries this line" — now ambiguous (both
  `c76076b3` and `933c891a` carry it) — and #04's apply commit
  (`933c891a`) is unmapped. This is the H16 class recurring; any
  per-SHA self-reference will keep regenerating each round.
- Proposed fix: replace the tail with explicit SHAs plus a terminal
  convention:
  "fix plan `…_#03.md` → `c76076b3`; `…_#04.md` → `933c891a`; later
  review-round fix plans map 1:1 to the `QS-288: apply review fix
  plan #NN` commits in this branch's history (see
  `git log --oneline`)."
  The trailing sentence stays true for #05 and any subsequent round —
  do NOT add a self-referential row for #05.

### [should-fix] K2 — Merge-note enumeration omits S-7; stop enumerating
- File: `docs/stories/QS-288.audit-report.md:7` (merge note)
- Severity: should-fix
- Source: qs-review-blind-hunter
- Description: the J3 fix kept an explicit list "(D-9, D-14, D-17,
  D-18, S-4, S-5, §2)" and missed S-7, whose evidence cites
  `quality_gate.py:1020` — the explicit list again tells the reader a
  stale anchor is merge-safe, the exact misdirection J3 was closing.
- Proposed fix: drop the enumeration entirely for a blanket phrase —
  "all `quality_gate.py` line citations in this report resolve
  against `7d2e5d93` only" — so the sentence cannot rot again.
  (Adding S-7 to the list is the inferior alternative.)

### [should-fix] K3 — Document the md-only `--quick tests/qs` gate rule in the contract files
- File: `.claude/commands/implement-setup-task.md:23-24` (expected
  outcome), `.claude/agents/qs-implement-setup-task.md:90-94`
  (`### 4` quality-gate section), and its mirrors
  `.cursor/agents/qs-implement-setup-task.md` +
  `.opencode/agents/qs-implement-setup-task.md` (three-mirror co-edit
  rule applies — see docs/workflow/project-rules.md "Harness sync")
- Severity: should-fix
- Source: qs-review-edge-case-hunter
- Description: for markdown-only change sets pinned by `tests/qs`,
  `--impacted` passes vacuously: testmon traces Python execution and
  cannot select doc-pinning tests for md changes, and diff-cover
  ignores non-Python files — so the gate goes green with zero tests
  while a pinned doc (e.g. a slash-command preamble asserted by
  `tests/qs/agents/test_slash_command_preambles.py`) is broken; the
  first failure surfaces only in CI. The audit report records exactly
  this hole and its workaround (report `:605-607`: doc-pinning tests
  run explicitly via `--quick` for the md-content batches), but no
  contract file does.
- Proposed fix: add one sentence to each of the four files (command +
  three agent mirrors), e.g.: "For md-only edits pinned by
  `tests/qs`, also run `python scripts/qs/quality_gate.py --quick
  tests/qs` before commit (testmon cannot select doc-pinning tests
  for markdown changes, so `--impacted` alone is vacuous there)."
  Keep the four wordings consistent; run the drift checker and
  `--quick tests/qs` itself before committing (this fix is itself an
  md-only edit pinned by tests/qs — eat the dog food).

### [nice-to-have] K4 — ha-battery.md: two identifiers for the discharge control
- File: `docs/agents/concepts/ha-battery.md:19` (TL;DR) vs `:60` (Key types)
- Severity: nice-to-have
- Source: qs-review-blind-hunter
- Description: the TL;DR says discharge is disabled "by driving
  `max_discharging_power` to 0" while the Key-types bullet names
  `max_discharge_number` — two identifiers for the same control in a
  doc rewritten for source-accuracy (F7 class). One is the domain
  attribute, the other the HA entity.
- Proposed fix: verify both names against source
  (`home_model/battery.py` attribute vs `ha_model/battery.py` entity
  config key), then either harmonize or state the distinction
  explicitly, e.g. "…by driving the `max_discharging_power` domain
  attribute to 0 (surfaced as the `max_discharge_number` HA entity)."

### [nice-to-have] K5 — AGENTS.md grammar omits `--full`
- File: `AGENTS.md:29-37` (commands block)
- Severity: nice-to-have
- Source: qs-review-blind-hunter
- Description: the block line 84 calls "the full grammar" omits
  `--full`, yet the report's own full-gate reference needed
  `--cache --full` and D-18 documents `--full` forcing scope — per
  §2, the bare "full gate" silently no-ops (0.12s) on a dev-only
  tree.
- Proposed fix: add a `--full` line consistent with the
  project-rules.md wording (verify the flag semantics against
  `scripts/qs/quality_gate.py` before writing).

### [nice-to-have] K6 — Stray `~` in fix plan #04's J8 file locator
- File: `docs/stories/QS-288.story_review_fix_#04.md:136-137` (J8)
- Severity: nice-to-have
- Source: qs-review-coderabbit (Minor, comment 3523202554 — the last
  open thread on the PR)
- Description: `docs/stories/QS-288.story_review_fix_#02.md:~141`
  renders the tilde as part of the path.
- Proposed fix: apply CodeRabbit's committable suggestion (drop the
  `~`). After pushing, reply briefly on thread 3523202554 so it can
  be resolved — that closes the PR's last open review thread.

### [nice-to-have] K7 — Confirm #295 covers the release.md parametrization
- File: n/a (GitHub issue metadata — check-only)
- Severity: nice-to-have
- Source: qs-review-acceptance-auditor
- Description: when follow-up #295 refreshes
  `tests/qs/agents/test_slash_command_preambles.py`'s now-historical
  exclusion comment, it should also add `release.md` to the
  parametrization so the new banner can't silently regress. Round-1
  F16 amended #295's body — this is a verification that the
  parametrization half is explicit there.
- Proposed fix: `gh issue view 295 --json body`; if the body already
  names both the stale comment AND adding `release.md` to the
  parametrization, do nothing. If the parametrization half is
  missing, append one sentence via `gh issue edit 295`.

## How to apply

Run `/implement-setup-task` against this fix plan. When done, return
and run `/review-task` again to re-verify.
