[% if harness == "claude" %]---
[% block frontmatter_claude %][% endblock %]
---
[% else %]---
[% block frontmatter_opencode %][% endblock %]
---
[% endif %]
<!-- GENERATED by scripts/qs/render_agents.py from scripts/qs/agent_templates/[[ template ]] — harness=[[ harness ]] lane=[[ lane or "none" ]] issue=[[ issue or "none" ]]. DO NOT EDIT; edit the template and re-render. -->

[% if orchestrator %]
[% set lane_none = "none (labels incomplete — see the Lane paragraph below)" if lane_aware else "none" %]
## Task facts (rendered)
[% if facts_state == "bound" %]
- Issue: #[[ issue ]] — [[ title ]]
- Branch: [[ branch ]] · Worktree: [[ worktree ]]
- Lane: [[ lane if lane else lane_none ]] · Story (path; may not exist yet): [[ story_file ]]

Volatile facts (PR number, latest review fix, story existence) are not rendered — run `python scripts/qs/context.py` for those.
[% elif facts_state == "lookup_failed" %]
Task QS_[[ issue ]] is bound but the issue lookup failed at render time — run `python scripts/qs/context.py`.
[% else %]
No task bound (branch `[[ branch or "unknown" ]]` is not `QS_<N>`). Run `python scripts/qs/context.py` from a task worktree.
[% endif %]

[% endif %]
[% block body %][% endblock %]
[% if orchestrator and lane_aware and lane_protocol %]

## Lane protocol (rendered from docs/workflow/lanes/[[ lane ]].md)

[[ lane_protocol ]]
[% endif %]
[% if orchestrator %]

## Reference map

Deterministic pointers (read on demand — not inlined, to keep this
prompt tight):

- `docs/workflow/project-rules.md` — the pipeline's hard rules (TDD,
  the quality gate, harness sync, doc maintenance).
- `docs/workflow/project-context.md` — what Quiet Solar is and how its
  pieces fit together.
- `docs/workflow/adversarial-review.md` — the 4-reviewer pattern the
  review phases run.
- `docs/workflow/harness.md` — the multi-harness abstraction (Claude /
  OpenCode / Codex) and the GUI launch surface.
- `docs/workflow/phase-protocols.md` (+ `docs/workflow/lanes/`) — every
  phase's contract and the per-lane variants.
- `docs/agents/index.md` — the maintenance docs for the pipeline's own
  code.
- `docs/stories/` — the per-task story files (`QS-<N>.story.md`).
[% endif %]
